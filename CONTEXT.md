# 领域词汇表

## 产品

- **AWAI创作工作台**：本应用面向用户展示的产品名称。
- **AWAI 官方支持入口**：由部署变量 `AWAI_SUPPORT_URL` 指定的客服或帮助页面；“关于”页不展示 GitHub、赞助或上游反馈链接。

## 图像提供商

- **Gemini 原生提供商**：通过用户所属 Sub2API 的 Gemini 原生中继生成或编辑图片的提供商类型；它使用 Gemini 请求和响应格式，不直连 Google，也不属于 OpenAI 兼容提供商。
- **Sub2API 限定提供商**：首发仅保留同一 Sub2API 地址上的 OpenAI 兼容图像、Responses 和 Gemini 原生协议；不提供 FAL 直连、Google 直连、任意自定义 HTTP 供应商或用户可编辑域名。
- **标准 Gemini 比例**：产品能力表中为 Gemini 图像模型配置的基础比例：`1:1`、`16:9`、`9:16`、`4:3`、`3:4`、`3:2`、`2:3`、`5:4`、`4:5`、`21:9`。具体模型是否启用由经验证的能力表决定。
- **扩展 Gemini 比例**：仅 `gemini-3.1-flash-image-preview` 可额外选择的比例：`8:1`、`4:1`、`1:4`、`1:8`。
- **自动 Gemini 比例**：客户端不传 `generationConfig.imageConfig.aspectRatio`，由模型自行决定图片比例；它不是值为 `auto` 的 API 参数。
- **预置 Gemini 模型**：首版在模型选择中提供 `gemini-3.1-flash-image-preview` 和 `gemini-3-pro-image-preview`；用户仍可手动填写其他模型 ID。
- **Gemini 分辨率选择**：界面仅提供自动、`1K`、`2K`、`4K`。自动时客户端省略 `generationConfig.imageConfig.imageSize`；模型不支持当前选择时回退为自动。
- **Agent 混合生图**：Agent 通过 Sub2API 的 `/v1/responses` 完成多轮对话和工具编排，并使用用户单独选择的图像 API 配置调用 Gemini 原生生图或 OpenAI 兼容的 `gpt-image-2` 等模型。
- **Agent 部分完成**：混合 Agent 已成功生成并保存图片，但后续 Responses 续写失败的轮次状态；已生成图片继续保留在画廊和轮次中，不因文本链路失败而删除。
- **Agent 继续回复**：从部分完成状态复用已保存图片和工具结果，仅重试 Responses 续写，绝不再次调用图像模型。
- **Agent 重新生成本轮**：用户明确要求重新执行整个 Agent 工具链的操作；可能再次调用图像模型和产生费用，执行前必须提示。

## 运行模式

- **在线嵌入版**：由 Sub2API 自定义菜单以 iframe 打开的运行模式；Sub2API 通过 URL 查询参数传入用户标识、登录 token、主题、语言和嵌入上下文。浏览器历史长期保留，云端素材副本固定保留 24 小时。
- **本地桌面版**：独立运行的运行模式；使用官方 Sub2API 逻辑域名，用户录入 API Key，素材保存在用户选择的素材库目录。
- **桌面版 Key 录入**：桌面首发由用户手动粘贴一个或多个 Sub2API Key；不提供 OAuth、Deeplink 或批量配置包导入。同一 Key 可供多个 API 配置复用，以兼容后续复合分组。
- **桌面全局凭据**：桌面版跨素材库复用的 Sub2API Key，明文只保存在 Windows 凭据管理器或 macOS 钥匙串；素材库元数据和 AWAI 备份包仅保存本机凭据 ID，不包含 Key 明文。
- **桌面单实例**：Windows 或 macOS 同一用户会话中只运行一个 AWAI 桌面进程；再次启动时唤醒已有窗口，不打开第二个实例。
- **桌面稳定更新**：桌面首发只有稳定版更新渠道；发现新版本后展示版本号和更新说明，由用户确认后下载、验签并安装。
- **桌面更新选择**：更新提示提供立即更新、稍后提醒和跳过此版本；应用不静默强制安装，不兼容版本只展示阻断原因和更新入口。
- **云端临时副本**：在线嵌入版上传至对象存储的生成结果原图及对应缩略图，生成后固定 24 小时到期；不包含参考图、遮罩或其他输入附件。
- **云端临时保存**：在线嵌入版默认开启的生成结果临时备份能力；首次进入明确说明云端保留 24 小时，用户可关闭后续上传或立即删除已有云端副本，并显示每份副本的准确到期时间。
- **云端单图上限**：每张生成结果原图上传至云端临时副本时最大为 30 MB；超限只使该图的云端上传失败，不影响浏览器本地保存和生成任务状态。
- **云端用户配额**：每名用户所有未到期云端临时副本的滚动总量上限为 1 GB；达到上限时停止新上传，不提前删除尚未到期的素材。
- **云端直传**：AWAI 在线服务验证菜单 JWT、文件声明和用户配额后，签发 10 分钟有效的一次性对象存储上传地址；浏览器不经 Node 服务中转文件内容，上传完成后由服务端校验对象并登记。
- **云端上传重试**：同一云端临时副本的对象上传或完成确认失败后最多自动重试两次；重试复用原对象键且不调用模型，两次失败后保留本地结果并允许用户手动重传。
- **AWAI 在线服务**：本仓库内独立部署的服务端，负责菜单 JWT 身份回验、临时素材元数据和云端副本生命周期；不建立 AWAI 凭据会话，也不负责 Sub2API 的模型计费。
- **桌面素材库目录**：本地桌面版存放 `generated/`、`references/`、`exports/` 与 `metadata/` 的用户指定目录；首发没有项目列表或项目切换概念，运行任务期间不得更改素材库位置。
- **默认素材库建议**：桌面首次启动预填系统“文档”目录下的 `AWAI创作工作台`，用户确认或更改位置后才创建目录和写入数据。
- **素材库迁移**：桌面版把当前数据库和素材复制到用户选择的新目录，校验数据库可打开且文件完整后才切换；旧目录不自动删除。
- **素材库恢复入口**：桌面启动时当前素材库不可访问所显示的阻断页面，只允许重试连接或重新定位已有素材库，不自动创建同名空目录。
- **桌面素材库数据库**：素材库 `metadata/awai.db` 中保存任务、收藏、Agent 对话和文件索引的 SQLite 数据库；图片二进制保存在普通文件中，备份包不直接复制该数据库。
- **Web 私有图片存储**：在线嵌入版使用 OPFS 保存原图和缩略图，IndexedDB 只保存任务元数据与文件索引；OPFS 不等同于用户可见素材库目录。
- **在线未保存结果**：图片已由模型成功生成，但因 OPFS 空间或写入错误未能持久化的状态；结果暂留当前页面内存供立即下载，不能显示为已本地保存。
- **在线版本提示**：在线版读取 AWAI 同源 `version.json`，页面重新获得焦点时检查；发现新版本且没有运行任务时提示刷新，有任务时延后提示。
- **Sub2API 控制台 JWT**：Sub2API 自定义菜单通过 iframe URL 传入、用于控制台用户接口认证的 token；它不是模型网关 API Key。
- **Sub2API 网关 API Key**：调用 Sub2API 图像和 Gemini 模型中继所需的独立用户凭据。
- **API 配置 Key 绑定**：在线版中，每个 Agent 文本、Gemini 或 OpenAI 兼容图像 API 配置分别绑定一个由用户明确选择的 Sub2API Key；不同配置可使用不同分组和价格的 Key。
- **在线版 Key 记忆**：按 `Sub2API 地址 + user_id + API 配置 ID` 保存对应 Key ID，不保存 Key 明文；每次进入时重新读取 Key 列表并逐项验证绑定状态。
- **按 Key 选模**：创建或编辑在线 API 配置时先选择一个 Key，再用该 Key 请求对应协议的模型列表并选择模型；多个 Key 的模型列表不合并，自定义模型 ID 通过独立入口填写。
- **官方 Sub2API 地址**：在线嵌入版使用 Sub2API 注入的 `src_host`；桌面版使用固定逻辑域名，不向用户展示备用节点选择。
- **Sub2API origin 允许列表**：在线部署配置的精确 HTTPS 主域名和备用域名集合；只有 `src_host` 命中时，AWAI 才能向该 origin 发送菜单 JWT 或 API Key。
- **首发数据空间**：AWAI创作工作台首次公开上线时创建的全新浏览器存储命名空间；不读取、不迁移也不清理旧应用的 IndexedDB、OPFS、localStorage 或缓存数据。
- **AWAI 备份包**：在线版与桌面版通用的 ZIP 交换格式，包含生成图片及提示词、模型、参数、实际尺寸、生成时间、收藏和 Agent 对话等元数据；用于用户主动备份、迁移和恢复，不承担自动云同步。
- **备份包导入副本**：导入时不覆盖当前素材库记录；ID 冲突的任务、图片、收藏或对话生成新 ID，并修正包内引用关系。
